"""Vendored third-party aethalometer calibration and data-processing code.

``calibration.py`` was copied from the local external file
``PKL data cleaning pipeline/calibration.py`` in git history commit ``8df009f``.
It contains code that imports and embeds pieces identified as ``aethpy``, but
the exact upstream and version were not recorded; see that commit's history.

This code is VENDORED third-party code. Do not edit it locally except for
documented patches. The known retained local patch is the top-level
``from tqdm import tqdm`` import in ``calibration.py``.
"""
